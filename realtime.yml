- name: Deploy realtime container
  community.docker.docker_container:
    name: "{{ realtime.name }}"
    image: "{{ realtime.repository }}:{{ realtime.version }}"
    env: "{{ realtime.env }}"
    networks:
      - name: "{{ docker_network }}"