- name: Deploy rest container
  community.docker.docker_container:
    name: "{{ rest.name }}"
    image: "{{ rest.repository }}:{{ rest.version }}"
    env: "{{ rest.env }}"
    networks:
      - name: supabase_net
    command:
      - "postgrest"