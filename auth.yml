- name: Deploy auth container
  community.docker.docker_container:
    name: "{{ auth.name }}"
    image: "{{ auth.repository }}:{{ auth.version }}"
    env: "{{ auth.env }}"
    networks:
      - name: supabase_net